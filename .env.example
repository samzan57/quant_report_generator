# Copier ce fichier en .env et renseigner ta propre clé.
# .env est ignoré par git — ne jamais committer de vraie valeur.

# Clé API Groq (https://console.groq.com) — nécessaire pour l'analyse IA de structure Excel
GROQ_API_KEY=
